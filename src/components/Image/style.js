import styled from 'styled-components';

const Img = styled.img`
  width: 100%;
  height: 50vh;
  margin-top: -10px;
  filter: blur(3px);
   -moz-filter: blur(3px);
   -webkit-filter: blur(3px);
   -o-filter: blur(3px);
   -ms-filter: blur(3px);
`;

export {Img};
